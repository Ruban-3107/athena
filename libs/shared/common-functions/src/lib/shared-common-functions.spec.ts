import { sharedCommonFunctions } from './shared-common-functions';

describe('sharedCommonFunctions', () => {
  it('should work', () => {
    expect(sharedCommonFunctions()).toEqual('shared-common-functions');
  });
});
